import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  imports: [CommonModule],
  selector: 'app-home-component',
  template: `Home component from remote 2 MFE`,
})
export class HomeComponent {}
